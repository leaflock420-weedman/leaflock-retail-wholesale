---
description: LL Wholesale isolated copy — never touch production LeafLock
globs: *
alwaysApply: true
---

# LeafLock Retail Wholesale — isolated project

This workspace is **LeafLock Retail Wholesale** at `C:\Users\wordo\LL-Wholesale`.

It is a **carbon copy** for separate use. It must **never** interfere with production:

| Production (do NOT edit from here) | This copy |
|--------------------------------------|-----------|
| `C:\Users\wordo\leaflock-pharmacy-wholesale` | `C:\Users\wordo\LL-Wholesale` |
| https://med.leaflock.com.au | http://localhost:4280 only |
| Render `srv-d93nossvikkc73amkvv0` | **Deploy blocked** |

## Rules for every agent session

1. **Only modify files inside `C:\Users\wordo\LL-Wholesale`** — never change the parent `leaflock-pharmacy-wholesale` folder unless the user explicitly opens that folder in a different chat.
2. **Never run Render deploy, DNS, or env-sync scripts** against production from this copy (`blockProductionDeploy` in `project.config.json`).
3. **Local dev port is 4280** — use `scripts\start-local.cmd`, not the original project's port.
4. If the user asks to deploy med.leaflock.com.au, tell them to open the **original** project in a **separate** Cursor window/chat.

Read `project.config.json` and `OPEN-SEPARATELY.txt` before any deploy or infrastructure task.